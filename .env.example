# PinxeSplit Environment Configuration
#
# This is a monorepo with separate .env files for each app:
#
# - apps/web/.env       -> Web app configuration (see apps/web/.env.example)
# - apps/api/.env       -> API server configuration (see apps/api/.env.example)
#
# To set up your environment:
#
#   cp apps/web/.env.example apps/web/.env
#   cp apps/api/.env.example apps/api/.env
#
# Then edit each .env file with your specific configuration.
